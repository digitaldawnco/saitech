/** @type {import('next').NextConfig} */
const nextConfig = {
     output: "standalone",
    //  basePath: '/',
    //  images: {
    //    unoptimized: true,
    //  },
};

export default nextConfig;
